<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="medical_email_15_template" model="mail.template">
            <field name="name">Delay e-mail template 15 day</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Congratz ${object.name}</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="auto_delete" eval="True"/>

            <field name="body_html">
                <![CDATA[
	          <p>Dear ${(object.name)},<br/><br/>
                  Thanks,<br/>
                  ${(object.company_id.name)}
	    ]]>
            </field>
        </record>
        <record id="medical_email_30_template" model="mail.template">
            <field name="name">Delay e-mail template 30 day</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Congratz ${object.name}</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html">
                <![CDATA[
	          <p>Dear ${(object.name)},<br/><br/>
                      Thanks,<br/>
                  ${(object.company_id.name)}
	    ]]>
            </field>
        </record>
        <record id="medical_email_60_template" model="mail.template">
            <field name="name">Delay e-mail template 45 day</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Congratz ${object.name}</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="auto_delete" eval="True"/>

            <field name="body_html">
                <![CDATA[
	          <p>Dear ${(object.name)},<br/><br/>
                  Thanks,<br/>
                  ${(object.company_id.name)}
	    ]]>
            </field>
        </record>
    </data>
</odoo>